export enum configuration {
    
}

export enum state {
    ACTIVE =  'ACTIVE',
    INACTIVE = 'INACTIVE ',
}
 

export enum roles {
    BUSINESS = 'BUSINESS',
    CLIENT = 'CLIENT',
    ADMIN  = 'ADMIN',
}

export enum jwt{
    JWT_SECRET = 'JWT_SECRET',
}